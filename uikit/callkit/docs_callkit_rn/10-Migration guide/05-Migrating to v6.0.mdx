# Migrating to v6.0

## Remove the permissions (Android only)

Since version 6.0, we no longer need the ACTION_MANAGE_OVERLAY_PERMISSION permission, so you can remove the call to ZegoUIKitPrebuiltCallService.requestSystemAlertWindow.
